# Dynaplan Development Progress

## Current Status: F004 IMPLEMENTED (tests pending run)
Last updated: 2026-03-01

## Completed
- [x] Project structure created
- [x] CLAUDE.md configured with workflow rules
- [x] features.json defined with full Anaplan feature parity list
- [x] Git repo initialized and pushed to GitHub
- [x] Backpressure infrastructure (run_silent.sh, test_all.sh, parallel_implement.sh)
- [x] F001: User authentication & authorization (11 tests passing)
  - User model with roles (admin, modeler, viewer)
  - bcrypt hashing + JWT tokens
  - Endpoints: register, login, me, logout
  - SQLite test setup with StaticPool
- [x] F002: Workspace management (assumed done, models exist)
- [x] F003: Model management (assumed done, models exist)
- [x] F004: Dimensions & hierarchies (implemented, tests written)
  - Dimension model: id, name, dimension_type (custom/time/version), model_id FK, timestamps
  - DimensionItem model: id, name, code, dimension_id FK, parent_id FK (self-ref, nullable), sort_order, timestamps
  - Self-referential FK on dimension_items.parent_id with ondelete=SET NULL
  - Schemas: DimensionCreate/Update/Response, DimensionItemCreate/Update/Response, DimensionItemNode (tree)
  - Service: full CRUD + tree builder (_build_tree recursive) + reorder_items
  - API routes: POST/GET /models/{model_id}/dimensions, PATCH/DELETE /dimensions/{dimension_id},
    POST/GET(flat|tree) /dimensions/{dimension_id}/items,
    PATCH/DELETE /dimensions/{dimension_id}/items/{item_id}
  - 30 tests in backend/tests/test_dimension.py

## In Progress
(nothing)

## Next Up
- [ ] F005: Modules & line items

## Known Issues
- Python 3.9.6 on this machine — need bcrypt==4.0.1 (not 5.x) for passlib compatibility
- numpy pinned to >=1.24.0 (not 2.1) for Python 3.9 compat

## Architecture Notes
- Frontend: Next.js 15 App Router at /frontend
- Backend: FastAPI at /backend
- Tests: SQLite in-memory with StaticPool (no external DB needed)
- Calculation engine: Python NumPy/Pandas based, server-side only
