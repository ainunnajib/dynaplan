[package]
name = "dynaplan-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon = "1.10"
uuid = { version = "1.8", features = ["v4"] }
chrono = { version = "0.4", default-features = false, features = ["alloc", "std"] }
